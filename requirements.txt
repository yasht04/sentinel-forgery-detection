# =============================================================
# requirements.txt
# Intelligent Media Forgery Detection — SENTINEL v3.0
# CSEDS 6th Semester Project
#
# INSTALL ALL:
#   pip install -r requirements.txt
#
# NOTE: If you are on Python 3.13, torch must be installed
# separately using the command below BEFORE running pip install:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# =============================================================

# ── Core Deep Learning ─────────────────────────────────────
torch==2.1.0
torchvision==0.16.0

# ── Model Architectures ────────────────────────────────────
transformers==4.40.0          # DistilBERT + CLIP (HuggingFace)
timm==0.9.16                  # EfficientNet-B4

# ── Image Processing ───────────────────────────────────────
opencv-python==4.9.0.80       # Image loading, resizing, warping
Pillow==10.3.0                # PIL Image handling
albumentations==1.3.1         # SBI augmentation (used in training)

# ── Numerical & Scientific ─────────────────────────────────
numpy==1.26.4                 # Array operations
scipy==1.13.0                 # DCT frequency analysis (Layer 3)

# ── Visualization ──────────────────────────────────────────
matplotlib==3.8.4             # Heatmap colormaps

# ── Web Interface ──────────────────────────────────────────
streamlit==1.35.0             # UI framework

# ── Utilities ──────────────────────────────────────────────
setuptools>=68.0.0            # Required by CLIP and other packages
ftfy==6.2.0                   # Text cleaning (used internally by CLIP)
regex==2024.4.16              # Regex engine (used internally by CLIP)

# =============================================================
# TRAINING-ONLY DEPENDENCIES (Google Colab — not needed locally)
# These are only required if you are re-training the model.
# They are pre-installed on Google Colab.
# =============================================================
# scikit-learn==1.4.2         # train/val/test split, metrics
# kaggle==1.6.14              # dataset download
# pandas==2.2.2               # DataFrame for dataset management

# =============================================================
# NOTES
# =============================================================
# Python version : 3.10 or 3.11 recommended (3.13 has CLIP issues)
# GPU            : Not required for inference (CPU is fine)
# GPU            : Required for training (Google Colab T4 used)
# CLIP weights   : Downloaded automatically on first run (~600MB)
# DistilBERT     : Downloaded automatically on first run (~268MB)
# EfficientNet   : Loaded from best_model.pth (your trained weights)